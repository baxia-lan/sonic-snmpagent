"""sonic-snmpagent Python wheel — migrated from rules/sonic-snmpagent.mk."""

load("//rules/bazel/wheel:wheel.bzl", "sonic_wheel")

package(default_visibility = ["//visibility:public"])

sonic_wheel(
    name = "sonic_snmpagent_wheel",
    distribution = "sonic-snmpagent",
    version = "1.0",
    summary = "SONiC SNMP agent for network management.",
    visibility = ["//visibility:public"],
)
